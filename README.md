# EduCode LMS Frontend

실시간 코딩 교육을 위한 학습 관리 시스템(LMS) 프론트엔드 프로토타입입니다.

## 프로젝트 개요

### 목적
교사와 학생이 실시간으로 코딩 학습을 진행할 수 있는 웹 기반 플랫폼입니다.

### 주요 특징
- **localStorage 기반 실시간 동기화**: 백엔드 없이 브라우저 간 상태 공유
- **교사-학생 역할 분리**: 각 역할에 맞는 UI/기능 제공
- **집중 모드**: 교사가 학생들의 화면을 동기화하여 수업 진행
- **실시간 화이트보드**: 교사의 판서를 학생들에게 실시간 전송
- **Monaco Editor 통합**: VS Code 수준의 코드 에디터 제공

---

## 기술 스택

| 분류 | 기술 | 버전 |
|------|------|------|
| Framework | Vue 3 (Composition API) | ^3.5.24 |
| State Management | Pinia | ^3.0.4 |
| Router | Vue Router | ^4.6.3 |
| Code Editor | Monaco Editor | ^0.55.1 |
| Utilities | VueUse | ^14.0.0 |
| Language | TypeScript | ^5.9.3 |
| Build Tool | Vite | ^7.2.4 |

---

## 프로젝트 구조

```
src/
├── assets/                    # 정적 자원 (이미지, 폰트 등)
│
├── components/                # Vue 컴포넌트
│   ├── collab/               # 협업 관련 컴포넌트
│   │   ├── SharedWhiteboard.vue  # 실시간 화이트보드
│   │   ├── TeamChat.vue          # 팀 채팅
│   │   └── TeamPresence.vue      # 참여자 현황
│   ├── common/               # 공통 컴포넌트
│   │   └── ThemeToggle.vue       # 다크/라이트 모드 토글
│   ├── content/              # 콘텐츠 표시 컴포넌트
│   │   └── ContentPane.vue       # 레슨 콘텐츠 패널
│   ├── editor/               # 에디터 컴포넌트
│   │   └── CodingPane.vue        # Monaco 코드 에디터 패널
│   ├── layout/               # 레이아웃 컴포넌트
│   │   └── SplitPane.vue         # 분할 패널 (드래그 리사이즈)
│   └── mobile/               # 모바일 전용 컴포넌트
│       └── MobileNavigator.vue   # 모바일 하단 네비게이션
│
├── composables/              # Vue Composables (재사용 가능한 로직)
│   ├── useLocalStorage.ts        # localStorage 동기화 훅
│   └── useStudentSession.ts      # 학생 세션 관리 훅
│
├── constants/                # 상수 정의
│   └── index.ts                  # 스토리지 키, 폴링 간격 등
│
├── data/                     # 정적 데이터
│   ├── koreanNames.ts            # 자동 생성용 한국 이름 목록
│   ├── lessonContent.ts          # 레슨 콘텐츠 (마크다운)
│   └── taskContent.ts            # 코딩 태스크 정의
│
├── router/                   # 라우터 설정
│   └── index.js                  # 라우트 정의
│
├── stores/                   # Pinia 스토어 (상태 관리)
│   ├── authStore.ts              # 역할 관리 (교사/학생)
│   ├── chatStore.ts              # 채팅 메시지
│   ├── codeShareStore.ts         # 코드 공유 상태
│   ├── focusModeStore.ts         # 집중 모드 상태
│   ├── lessonStore.ts            # 레슨 단계 관리
│   ├── studentStore.ts           # 학생 목록/코드 관리
│   ├── themeStore.ts             # 테마 (다크/라이트)
│   ├── uiStore.ts                # UI 상태 (모바일 뷰 등)
│   ├── whiteboardStore.ts        # 화이트보드 동기화
│   ├── sync.ts                   # 통합 스토어 (호환성 레이어)
│   └── index.ts                  # 스토어 내보내기
│
├── types/                    # TypeScript 타입 정의
│   └── index.ts                  # 공통 타입 (Student, ChatMessage 등)
│
├── utils/                    # 유틸리티 함수
│   ├── cursorStyles.ts           # 커서 스타일 주입
│   ├── generators.ts             # ID 생성기
│   └── storage.ts                # localStorage 헬퍼
│
├── views/                    # 페이지 컴포넌트
│   ├── LearningInterface.vue     # 학생 학습 화면
│   └── teacher/
│       └── TeacherCourseView.vue # 교사 강의 화면
│
├── App.vue                   # 루트 컴포넌트
├── main.js                   # 앱 진입점
├── style.css                 # 전역 스타일
└── env.d.ts                  # 환경 타입 정의
```

---

## 라우트 구조

| 경로 | 이름 | 컴포넌트 | 설명 |
|------|------|----------|------|
| `/` | - | - | `/student/course`로 리다이렉트 |
| `/student/course` | student-course | LearningInterface | 학생 학습 화면 |
| `/teacher/course` | teacher-course | TeacherCourseView | 교사 강의 화면 |

---

## 핵심 기능 상세

### 1. localStorage 기반 실시간 동기화

백엔드 서버 없이 브라우저의 localStorage를 활용하여 탭/창 간 상태를 동기화합니다.

```typescript
// constants/index.ts
export const STORAGE_PREFIX = 'educode:sync:'
export const POLLING_INTERVAL = 300  // 300ms 간격으로 폴링
```

**동기화 메커니즘:**
- `storage` 이벤트: 다른 탭에서의 변경 감지
- 폴링: 같은 탭 내 iframe 등을 위한 주기적 체크

### 2. 역할 기반 기능 분리

| 기능 | 교사 | 학생 |
|------|:----:|:----:|
| 집중 모드 제어 | O | X |
| 화이트보드 그리기 | O | X (읽기 전용) |
| 학생 코드 모니터링 | O | X |
| 레슨 단계 변경 | O | X |
| 코드 작성 | O | O |
| 채팅 | O | O |

### 3. 집중학습 (Focus Mode)

UI에서는 "집중학습"으로 표시됩니다. 교사가 활성화하면:
- 학생들의 콘텐츠 스크롤이 교사와 동기화
- 학생들의 레슨 단계가 교사와 동기화
- 화이트보드 판서가 실시간 전송

**화이트보드 도구:**
| 도구 | 아이콘 | 설명 |
|------|--------|------|
| 선택 (커서) | 🖱️ | 판서 모드 해제, 콘텐츠 클릭 가능 |
| 펜 | ✏️ | 자유 드로잉 (색상 선택 가능) |
| 형광펜 | 🖍️ | 반투명 하이라이트 (색상 선택 가능) |
| 지우개 | 🧹 | 획 단위로 삭제 |
| 전체 지우기 | 🗑️ | 모든 판서 삭제 |

- 도구바는 드래그하여 위치 이동 가능
- 기본값: 선택 도구 (커서 모드)
- **판서 내역 유지**: 판서 모드를 끄더라도 이전에 그린 판서 내역은 교사/학생 화면에 계속 표시됨 (집중 모드가 활성화된 경우)
- **페이지별 판서 분리**: 각 레슨 단계(페이지)마다 판서 내역이 독립적으로 저장됨. 한 페이지에서 그린 판서는 다른 페이지에서 보이지 않음.

**판서 동기화 모드:**
- 집중 모드 + 판서 모드가 모두 활성화된 경우
- 학생의 콘텐츠 영역 너비가 교사의 콘텐츠 영역 너비와 동일하게 고정됨
- 판서 좌표가 콘텐츠 너비 기준으로 정규화되어 정확하게 표시됨
- 데스크톱/모바일 모두 동일하게 적용
- 학생 화면 너비가 교사 콘텐츠 너비보다 작은 경우:
  - 콘텐츠 영역에 좌우 스크롤 발생
  - 실습(코딩) 영역은 유지됨 (데스크톱: 최소 200px, 모바일: 탭 전환)

### 4. 목차 및 전체 커리큘럼

**현재 차시 목차:**
- 목차 아이콘 클릭 시 현재 차시의 수업 목록 드롭다운 표시
- 이전/다음 버튼으로 단계 이동

**전체 목차:**
- 드롭다운 우측 상단의 "전체 목차" 버튼 클릭
- 전체 커리큘럼을 단원 > 차시 > 수업 계층 구조로 표시
- 현재 학습중인 차시 하이라이트 및 "현재 학습중" 배지 표시
- 완료/진행중/예정 상태 아이콘으로 구분
- 클릭하여 해당 수업으로 바로 이동 가능

**커리큘럼 구조 예시:**
```
단원 1: 자바 프로그래밍 입문
  └─ 1차시: 자바 기초 문법 [현재 학습중]
       ├─ 자바 프로그래밍 시작하기
       ├─ Hello World 출력하기
       ├─ 변수와 데이터 타입
       ├─ 조건문 (if-else)
       └─ 반복문 (for 루프)
```

**레슨 단계 이동 시:**
- 이전/다음 단계 버튼 클릭 시 콘텐츠 스크롤이 자동으로 최상단으로 이동
- 교사 모드에서는 스크롤 위치가 학생들에게도 동기화됨

**학생 자유 학습:**
- 집중 모드가 꺼져있으면 학생도 자유롭게 단계를 이동할 수 있음
- 집중 모드가 켜지면 교사의 단계에 동기화됨

### 4. 학생 코드 모니터링 (교사 전용)

교사가 개별 학생의 실습 코드를 실시간으로 확인할 수 있습니다.

**기능:**
- 모니터링 버튼 클릭 → 학생 선택 → 해당 학생의 코드 탭 생성
- 학생 코드 실시간 폴링 (300ms 간격)
- 모니터링 중인 학생 코드를 다른 학생들에게 공유 가능

**동작 규칙:**
- 공유 중인 학생의 모니터링 탭을 닫으면 공유가 자동으로 중단됨
- 여러 학생을 동시에 모니터링 가능 (탭으로 전환)

### 5. Monaco Editor 통합

- Java 언어 지원 (문법 하이라이팅)
- 다크/라이트 테마 연동
- 실시간 코드 동기화 (학생 → 교사)

---

## 스토어 아키텍처

### 분리된 스토어 구조

```
┌─────────────────────────────────────────────────────────────┐
│                        sync.ts                               │
│              (통합 스토어 - 호환성 레이어)                      │
│  ┌──────────┬──────────┬──────────┬──────────┬──────────┐   │
│  │ authStore│focusMode │codeShare │whiteboard│ student  │   │
│  │          │  Store   │  Store   │  Store   │  Store   │   │
│  └──────────┴──────────┴──────────┴──────────┴──────────┘   │
│  ┌──────────┬──────────┬──────────┬──────────┐              │
│  │  lesson  │   chat   │    ui    │  theme   │              │
│  │  Store   │  Store   │  Store   │  Store   │              │
│  └──────────┴──────────┴──────────┴──────────┘              │
└─────────────────────────────────────────────────────────────┘
```

### 각 스토어 역할

| 스토어 | 파일 | 역할 |
|--------|------|------|
| Auth | authStore.ts | 교사/학생 역할 설정 |
| FocusMode | focusModeStore.ts | 집중 모드 상태, 스크롤 위치 |
| CodeShare | codeShareStore.ts | 코드 공유 활성화 상태 |
| Whiteboard | whiteboardStore.ts | 페이지별 판서 경로, 현재 그리기 상태 |
| Student | studentStore.ts | 학생 목록, 학생별 코드 |
| Lesson | lessonStore.ts | 현재 레슨 단계, 콘텐츠 |
| Chat | chatStore.ts | 채팅 메시지 목록 |
| UI | uiStore.ts | 모바일 뷰 모드, 분할 비율 |
| Theme | themeStore.ts | 다크/라이트 테마 |

---

## 실행 방법

### 개발 서버 실행

```bash
npm install
npm run dev
```

### 프로덕션 빌드

```bash
npm run build
npm run preview
```

### 테스트 방법

1. 브라우저 탭 2개 열기
2. 탭 1: `http://localhost:5173/teacher/course` (교사)
3. 탭 2: `http://localhost:5173/student/course` (학생)
4. 교사 화면에서 집중 모드 활성화 후 기능 테스트

---

## 개발 가이드라인

### 컴포넌트 작성 규칙

```vue
<script setup>
// 1. Vue imports
import { ref, computed, onMounted } from 'vue'

// 2. 컴포넌트 imports
import SomeComponent from './SomeComponent.vue'

// 3. 스토어 imports
import { useSyncStore } from '@/stores/sync'

// 4. 타입 imports
import type { Student } from '@/types'

// 5. 로직 구현
const store = useSyncStore()
</script>

<template>
  <!-- 템플릿 -->
</template>

<style scoped>
/* 스코프드 스타일 */
</style>
```

### 스토어 작성 규칙

```typescript
// stores/exampleStore.ts
import { ref, computed } from 'vue'
import { defineStore } from 'pinia'
import { STORAGE_KEYS } from '@/constants'
import { getStorageValue, setStorageValue } from '@/utils/storage'

export const useExampleStore = defineStore('example', () => {
  // State
  const value = ref(getStorageValue(STORAGE_KEYS.EXAMPLE, 'default'))

  // Getters (computed)
  const derivedValue = computed(() => value.value.toUpperCase())

  // Actions (functions)
  function setValue(newValue: string): void {
    value.value = newValue
    setStorageValue(STORAGE_KEYS.EXAMPLE, newValue)
  }

  return { value, derivedValue, setValue }
})
```

### 역할 설정 (중요)

교사/학생 역할은 **반드시 스크립트 레벨**에서 설정해야 합니다. `onMounted`에서 설정하면 자식 컴포넌트가 먼저 마운트되어 역할 체크가 실패할 수 있습니다.

```vue
<script setup>
const syncStore = useSyncStore()

// ✅ 올바른 방법: 스크립트 레벨에서 즉시 설정
syncStore.setTeacherRole(false)  // 또는 true

// ❌ 잘못된 방법: onMounted에서 설정
// onMounted(() => {
//   syncStore.setTeacherRole(false)  // 자식 컴포넌트 마운트 후 실행됨
// })
</script>
```

**이유**: Vue 3에서 자식 컴포넌트의 `onMounted`가 부모의 `onMounted`보다 먼저 실행됩니다. 따라서 CodingPane 등 자식 컴포넌트가 역할을 체크할 때 아직 역할이 설정되지 않은 상태일 수 있습니다.

### localStorage 키 추가 시

1. `constants/index.ts`에 키 추가
2. `types/index.ts`에 필요한 타입 추가
3. 해당 스토어에서 사용

---

## CSS 변수 (테마)

```css
/* 다크 모드 (기본) */
--bg-primary: #0f172a;
--bg-secondary: #1e293b;
--bg-tertiary: #334155;
--text-primary: #f8fafc;
--text-secondary: #94a3b8;
--text-accent: #38bdf8;
--border-color: rgba(148, 163, 184, 0.1);
--glass-bg: rgba(30, 41, 59, 0.7);
--glass-border: rgba(255, 255, 255, 0.1);
--primary-color: #3b82f6;
--primary-hover: #2563eb;
--success-color: #10b981;
--danger-color: #ef4444;
--warning-color: #f59e0b;
--info-color: #60a5fa;
--purple-color: #a855f7;
--hover-bg: rgba(255, 255, 255, 0.1);
--active-bg: rgba(255, 255, 255, 0.15);
--disabled-bg: rgba(255, 255, 255, 0.05);
--shadow-lg: 0 4px 12px rgba(0, 0, 0, 0.3);
--shadow-xl: 0 10px 25px rgba(0, 0, 0, 0.5);
--overlay-bg: rgba(0, 0, 0, 0.6);

/* 라이트 모드 */
--bg-primary: #ffffff;
--bg-secondary: #f8fafc;
--bg-tertiary: #e2e8f0;
--text-primary: #0f172a;
--text-secondary: #64748b;
--text-accent: #0284c7;
--border-color: #cbd5e1;
--glass-bg: rgba(255, 255, 255, 0.9);
--glass-border: rgba(0, 0, 0, 0.1);
--hover-bg: rgba(0, 0, 0, 0.05);
--active-bg: rgba(0, 0, 0, 0.1);
--disabled-bg: rgba(0, 0, 0, 0.03);
--shadow-lg: 0 4px 12px rgba(0, 0, 0, 0.1);
--shadow-xl: 0 10px 25px rgba(0, 0, 0, 0.15);
--overlay-bg: rgba(0, 0, 0, 0.4);
```

---

## 알려진 제한사항

1. **localStorage 용량**: 브라우저별 5-10MB 제한
2. **동일 도메인 필수**: localStorage는 같은 도메인에서만 공유
3. **탭 간 동기화 지연**: 폴링 간격(300ms)만큼의 지연 발생
4. **Monaco Editor 번들 크기**: 프로덕션 빌드 시 큰 청크 생성

---

## 변경 이력

### 2024-11-26: 클린 아키텍처 리팩토링

**주요 변경사항:**
- JavaScript → TypeScript 전환
- 단일 sync.js (553줄) → 9개 분리된 스토어로 리팩토링
- types, constants, utils, data, composables 디렉토리 구조화
- 호환성 레이어(sync.ts) 추가로 기존 컴포넌트 인터페이스 유지

**삭제된 파일:**
- `src/stores/sync.js` (sync.ts로 대체)
- `src/stores/lesson.js` (lessonStore.ts로 대체)
- `src/stores/ui.js` (uiStore.ts로 대체)
- `src/stores/theme.js` (themeStore.ts로 대체)
- `src/stores/collab.js` (chatStore.ts 등으로 분리)
- `src/stores/cursorStyles.js` (utils/cursorStyles.ts로 이동)
- `vite.config.js` (vite.config.ts로 대체)

**신규 파일:**
- `src/types/index.ts` - 타입 정의
- `src/constants/index.ts` - 상수 정의
- `src/utils/storage.ts` - localStorage 헬퍼
- `src/utils/generators.ts` - ID 생성기
- `src/utils/cursorStyles.ts` - 커서 스타일
- `src/data/lessonContent.ts` - 레슨 콘텐츠
- `src/data/taskContent.ts` - 태스크 데이터
- `src/data/koreanNames.ts` - 이름 목록
- `src/composables/useLocalStorage.ts` - localStorage 훅
- `src/composables/useStudentSession.ts` - 학생 세션 훅
- `src/stores/*.ts` - 9개의 분리된 TypeScript 스토어

---

## 향후 개선 계획

- [ ] 백엔드 서버 연동 (WebSocket 기반 실시간 동기화)
- [ ] 사용자 인증 시스템
- [ ] 다중 강의실 지원
- [ ] 코드 실행 기능 (백엔드 필요)
- [ ] 학생 진도 추적 및 분석

---

## 라이선스

Private - 내부 사용 전용
